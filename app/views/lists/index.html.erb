<h1>All todo lists</h1>

<% @lists.each do |list| %>

<h3><%= link_to list.name, list_path(list) %></h3>
<p><%= link_to "Delete", list_path(list), :method => :delete %> | <%= link_to "Edit", edit_list_path(list) %></p>
<% end %>

<hr/>

<%= render :partial => 'form' %>

<%= link_to "New list", new_list_path ,
									class: "btn" %>
